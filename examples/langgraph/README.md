# LangGraph Example

Placeholder for LangGraph + OxDeAI integration example.

Planned scope:
- pre-execution `evaluatePure(...)` gating
- deterministic state commit
- audit + envelope verification flow
