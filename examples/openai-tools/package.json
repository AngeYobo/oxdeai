{
  "name": "@oxdeai/example-openai-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OxDeAI pre-execution economic boundary demo - OpenAI Tools pattern",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "sh -c 'pnpm build > /dev/null 2>&1 && clear && node dist/run.js'",
    "demo": "sh -c 'pnpm build && node dist/run.js'",
    "validate": "node ../../scripts/validate-adapters.mjs --one openai-tools",
    "start:deny": "sh -c 'pnpm build && DEMO_DENY=1 node dist/run.js'",
    "demo:openai-tools": "sh -c 'pnpm -C examples/openai-tools build && node examples/openai-tools/dist/run.js'",
    "demo:openai": "sh -c 'pnpm build > /dev/null 2>&1 && clear && NODE_NO_WARNINGS=1 node dist/run-openai.js'",
    "demo:gif": "vhs ../../docs/media/openai-demo.tape"
  },
  "dependencies": {
    "@oxdeai/core": "workspace:*",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
