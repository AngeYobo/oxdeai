{
  "name": "@oxdeai/guard",
  "version": "1.0.0",
  "description": "Universal execution authorization guard (PEP boundary) for OxDeAI",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@oxdeai/core": "workspace:*"
  },
  "devDependencies": {
    "@oxdeai/sdk": "^1.3.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "pnpm build && node --test \"dist/test/guard.test.js\" \"dist/test/guard.property.test.js\""
  }
}
