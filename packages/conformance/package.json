{
  "name": "@oxdeai/conformance",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "extract": "pnpm build && node dist/scripts/extract-vectors.js",
    "validate": "pnpm build && node dist/src/validate.js"
  },
  "dependencies": {
    "@oxdeai/core": "^1.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
