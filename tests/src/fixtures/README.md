# Test Fixtures

TEST ONLY — DO NOT USE IN PRODUCTION.

This directory contains non-production fixture material for integration/unit tests.

Rules:

- Never commit production secrets or private keys.
- Cryptographic fixtures are allowed only under fixture paths and must be explicitly labeled.
- Prefer runtime-generated keypairs where deterministic fixed fixtures are not required.
